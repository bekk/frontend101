// application


// Denne funksjonen kalles når siden er ferdig lastet
var app = function() {
    var tag = "trondheim";

    // Flickr.search(tag, function (items) {
    // ...
    // });
};

// startup

$(document).ready(app);
